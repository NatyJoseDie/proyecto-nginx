import { FormatAnswerInterceptorInterceptor } from './format-answer-interceptor.interceptor';

describe('FormatAnswerInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new FormatAnswerInterceptorInterceptor()).toBeDefined();
  });
});
