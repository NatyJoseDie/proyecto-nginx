import { ValidateAnswerInterceptor } from './validate-answer.interceptor';

describe('ValidateAnswerInterceptor', () => {
  it('should be defined', () => {
    expect(new ValidateAnswerInterceptor()).toBeDefined();
  });
});
